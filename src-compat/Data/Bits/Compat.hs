{-# LANGUAGE CPP #-}
module Data.Bits.Compat (
    popCount,
    zeroBits,
    finiteBitSize,
    countLeadingZeros,
    ) where

import Data.Bits (popCount, zeroBits, finiteBitSize, countLeadingZeros)
